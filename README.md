# 国庆美东行程规划 — 部署说明

## 项目文件
- `6p0xM6Bn.html`：行程手册单文件（离线可用）
- `index.html`：访问口令页（口令以哈希校验，源码不含明文）

## 更新部署
```bash
git add -A
git commit -m "更新内容说明"
git push
```
推送后 GitHub Pages 自动重新构建，约 1-2 分钟生效。

## 访问方式
打开站点首页 → 输入访问口令 → 进入行程手册。
口令不写入本仓库，请通过私密渠道单独传递。
